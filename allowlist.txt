# 允许访问内部数据看板的企业微信 UserId 白名单
# 一行一个，# 开头为注释
#
# UserId 获取方式（任选其一）：
#   1. 企业微信管理后台 -> 通讯录 -> 成员 -> 点开成员 -> 「账号」字段即为 UserId
#   2. 让用户先访问一次看板，查看服务日志 /opt/xnkq-kanban/logs/auth.log，
#      日志里会记录 "拒绝登录：UserId xxx 不在白名单"，把 xxx 加到这里即可
#
# 说明：
#   - 企业微信应用的「可见范围」是第一道防线（看不到应用入口）
#   - 本白名单是第二道防线（即使拿到链接也进不来）
#   - 离职人员请务必从此文件移除
#
# 修改后无需重启服务，下次访问自动生效（白名单每次请求都会重新读取）

# 示例：
# ZhangSan
# LiSi
# WangWu
